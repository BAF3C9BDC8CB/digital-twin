# dt sense（环境感知命令）实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增 `dt sense [path] [--json]` + MCP `dt_sense`：确定性完成"定位→注册匹配→索引状态→项目简报/发现报告"，替代 AGENTS.md 中依赖 AI 多步分析的环境感知流程。

**Architecture:** 只读命令。`SenseService`（`src/application/sense/`）编排：locate（config.yaml 最长前缀匹配）→ status（三态：indexed/registered_not_indexed/unregistered）→ brief（Qdrant scroll payload 聚合目录画像/语言 + Memgraph 统计/关键实体 + SQLite 最近构建）→ discover（未注册时一级子目录候选扫描）。CLI 渲染壳；MCP subprocess 透传 JSON。

**Tech Stack:** Rust（tokio/async-trait/serde/clap/qdrant_client）、Qdrant（scroll API）、Memgraph（Cypher）、SQLite（snapshots）、Python MCP（subprocess）。

**Spec:** `docs/superpowers/specs/2026-08-03-dt-sense-design.md`（S-D1..S-D7 已定稿）

## Global Constraints

- 测试基线：**675 passed / 2 failed 预存**（`infrastructure::parser::ts_java::tests::parses_hello_service`、`interfaces::cli::backup_sqlite::tests::copy_database_writes_file`）——任何任务结束失败数不得增加；`cargo clippy` 0 error
- live 测试一律 `#[ignore]`（需 Memgraph `:7688` + Qdrant `:6333/:6334` 在线）；离线 `cargo test` 不受影响
- 每任务一次提交：`feat(dt-sense): ...` / `docs(dt-sense): ...`
- TDD：行为变更先写失败测试再实现
- sense **永不失败退出**（除 path 不存在）：后端不可用 → `degraded` 标注，退出码 0
- library 不读 config.yaml：`projects: Vec<(String, PathBuf)>` 由 main.rs `load_config()`+`resolve_project_paths()`（main.rs :473/:497）解析后传入

---

### Task 1: VectorRepository::scroll_payloads 接口 + Qdrant 实现

**Files:**
- Modify: `src/domain/traits.rs`（trait VectorRepository :34 起，在 `health_check` 声明后追加）
- Modify: `src/infrastructure/qdrant/repo.rs`（override 实现；复用同文件 :374 `json_to_qdrant_filter`；tests 模块 :434 起）

**Interfaces:**
- Consumes: `json_to_qdrant_filter(&serde_json::Value) -> Result<qdrant_client::qdrant::Filter, DtError>`（repo.rs :374）；client 风格 `self.client.inner()` → `qdrant.scroll_points(...)`
- Produces（T3 依赖）:
```rust
// trait VectorRepository 内（有默认实现，不破坏现有 stub）：
/// Scroll all point payloads in a collection (no vectors), optionally filtered.
/// Returns raw payload JSON objects. Default: empty (graceful degradation).
async fn scroll_payloads(
    &self,
    collection: &str,
    filter: Option<serde_json::Value>,
    max: usize,
) -> Result<Vec<serde_json::Value>, crate::domain::error::DtError> {
    let _ = (collection, filter, max);
    Ok(Vec::new())
}
```

- [x] **Step 1: 写失败测试**

`src/infrastructure/qdrant/repo.rs` tests 模块（:493 `noop_uses_default_search_with_filter` 附近）追加：

```rust
#[tokio::test]
async fn noop_scroll_payloads_returns_empty() {
    let repo = NoopVectorRepo;
    let out = repo
        .scroll_payloads("code_methods", None, 100)
        .await
        .expect("default scroll impl");
    assert!(out.is_empty());
}
```

- [x] **Step 2: 运行确认失败**

Run: `cargo test noop_scroll_payloads_returns_empty 2>&1 | tail -5`
Expected: FAIL — compile error `no method named scroll_payloads`

- [x] **Step 3: trait 加默认方法 + Qdrant override**

traits.rs：把 Produces 中的默认方法插到 `async fn health_check(...)` 声明之后（`payload_matches_filter` 自由函数之前）。

repo.rs `impl VectorRepository for QdrantVectorRepo` 块内（`search_with_filter` :145 之后）追加：

```rust
async fn scroll_payloads(
    &self,
    collection: &str,
    filter: Option<serde_json::Value>,
    max: usize,
) -> Result<Vec<serde_json::Value>, DtError> {
    use qdrant_client::qdrant::ScrollPointsBuilder;

    let qdrant = self.client.inner();
    let mut out = Vec::new();
    let mut offset: Option<qdrant_client::qdrant::PointId> = None;
    loop {
        let mut builder = ScrollPointsBuilder::new(collection.to_string())
            .limit(256)
            .with_payload(true)
            .with_vectors(false);
        if let Some(f) = &filter {
            builder = builder.filter(json_to_qdrant_filter(f)?);
        }
        if let Some(o) = offset {
            builder = builder.offset(o);
        }
        let resp = qdrant
            .scroll_points(builder)
            .await
            .map_err(|e| DtError::Repository(format!("Qdrant scroll_payloads: {e}")))?;
        for point in resp.result {
            let payload: serde_json::Value = serde_json::to_value(&point.payload)
                .unwrap_or_else(|_| serde_json::json!({}));
            out.push(payload);
            if out.len() >= max {
                return Ok(out);
            }
        }
        match resp.next_page_offset {
            Some(next) => offset = Some(next),
            None => break,
        }
    }
    Ok(out)
}
```

注意：`point.payload` 类型是 `HashMap<String, qdrant_client::qdrant::Value>`（prost Value），`serde_json::to_value` 对 prost Value 不直接可用——参照同文件 `scored_points_to_json` 的 payload 转换方式（若其中有 prost→serde 的转换辅助函数则复用同一个；若没有，用 `serde_json::to_value` 先试编译，失败则按 `scored_points_to_json` 中实际转换路径抽出共用 helper）。以实现时 `scored_points_to_json` 的真实代码为准。

- [x] **Step 4: 运行测试通过 + 回归**

Run: `cargo test noop_scroll_payloads 2>&1 | tail -3 && cargo test --lib 2>&1 | tail -3 && cargo clippy 2>&1 | grep -E "^error" | head -3`
Expected: 新测试 PASS；lib 675 passed / 2 预存失败不变；clippy 无 error

- [x] **Step 5: Commit**

```bash
git add src/domain/traits.rs src/infrastructure/qdrant/repo.rs
git commit -m "feat(dt-sense): VectorRepository::scroll_payloads 默认方法 + Qdrant scroll 实现"
```

---

### Task 2: sense 模块骨架 + locate 最长前缀匹配

**Files:**
- Create: `src/application/sense/mod.rs`
- Create: `src/application/sense/locate.rs`
- Modify: `src/application/mod.rs`（加 `pub mod sense;`）

**Interfaces:**
- Consumes: `&[(String, PathBuf)]`（T7 由 main.rs 传入）
- Produces（T3 依赖）:
```rust
// locate.rs
/// config.yaml 项目列表中最长前缀匹配；path 必须先 canonicalize（调用方负责）。
pub fn match_project<'a>(
    path: &std::path::Path,
    projects: &'a [(String, std::path::PathBuf)],
) -> Option<(&'a str, &'a std::path::Path)>;

/// 从 path 向上找含 .git 的目录（用于提示，不参与归属判定）。
pub fn find_git_root(path: &std::path::Path) -> Option<std::path::PathBuf>;
```

- [x] **Step 1: 写失败测试**

`src/application/sense/locate.rs`：

```rust
//! 项目定位：git root + config.yaml 项目最长前缀匹配。

use std::path::{Path, PathBuf};

pub fn match_project<'a>(path: &Path, projects: &'a [(String, PathBuf)]) -> Option<(&'a str, &'a Path)> {
    projects
        .iter()
        .filter(|(_, root)| path.starts_with(root))
        .max_by_key(|(_, root)| root.as_os_str().len())
        .map(|(n, r)| (n.as_str(), r.as_path()))
}

pub fn find_git_root(path: &Path) -> Option<PathBuf> {
    let mut cur = Some(path);
    while let Some(p) = cur {
        if p.join(".git").exists() {
            return Some(p.to_path_buf());
        }
        cur = p.parent();
    }
    None
}

#[cfg(test)]
mod tests {
    use super::*;

    fn projects() -> Vec<(String, PathBuf)> {
        vec![
            ("warehouse".into(), PathBuf::from("/data/aflm/warehouse")),
            ("warehouse-api".into(), PathBuf::from("/data/aflm/warehouse/warehouse-api")),
            ("dt".into(), PathBuf::from("/data/myProject/digital-twin-v2")),
        ]
    }

    #[test]
    fn longest_prefix_wins_for_nested_project() {
        let p = Path::new("/data/aflm/warehouse/warehouse-api/src/main");
        let (name, root) = match_project(p, &projects()).unwrap();
        assert_eq!(name, "warehouse-api");
        assert_eq!(root, Path::new("/data/aflm/warehouse/warehouse-api"));
    }

    #[test]
    fn falls_back_to_parent_project() {
        let p = Path::new("/data/aflm/warehouse/shared-lib");
        let (name, _) = match_project(p, &projects()).unwrap();
        assert_eq!(name, "warehouse");
    }

    #[test]
    fn unregistered_returns_none() {
        let p = Path::new("/home/luis/other");
        assert!(match_project(p, &projects()).is_none());
    }

    #[test]
    fn exact_root_matches() {
        let p = Path::new("/data/myProject/digital-twin-v2");
        assert_eq!(match_project(p, &projects()).unwrap().0, "dt");
    }
}
```

`src/application/sense/mod.rs`：

```rust
//! dt sense —— 环境感知（只读）：定位 → 状态 → 简报/发现报告。

pub mod locate;
```

`src/application/mod.rs` 按字母序加 `pub mod sense;`。

- [x] **Step 2: 运行确认失败**

Run: `cargo test locate 2>&1 | tail -5`
Expected: FAIL（模块不存在 → 编译错误）

- [x] **Step 3: 按上面代码实现**

（Step 1 代码已含实现；此步确认无误即可——TDD 此处测试与实现同帧给出，因为函数是纯逻辑无外部依赖。）

- [x] **Step 4: 运行测试通过**

Run: `cargo test locate 2>&1 | tail -3`
Expected: 4 passed

- [x] **Step 5: Commit**

```bash
git add src/application/sense/ src/application/mod.rs
git commit -m "feat(dt-sense): sense 模块骨架 + locate 最长前缀匹配"
```

---

### Task 3: SenseReport 类型 + SenseService 三态判定 + stats

**Files:**
- Modify: `src/application/sense/mod.rs`（类型 + SenseService + tests/stubs）

**Interfaces:**
- Consumes: T1 `scroll_payloads`；T2 `match_project`；`GraphRepository::read_query(&self, query: &str, params: HashMap<String, serde_json::Value>) -> Result<serde_json::Value, DtError>`；`SnapshotRepository::list_snapshots(&self, project: &str) -> Result<Vec<FileSnapshot>, DtError>`（`FileSnapshot.updated_at: String` ISO8601，domain/types.rs :346）
- Produces（T4/T5/T6/T7/T8 全部依赖——本任务即最终契约，后续任务只填空）:

```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, serde::Serialize)]
#[serde(rename_all = "snake_case")]
pub enum SenseStatus { Indexed, RegisteredNotIndexed, Unregistered }

#[derive(Debug, Clone, serde::Serialize)]
pub struct ProjectRef { pub name: String, pub path: String, pub registered: bool }

#[derive(Debug, Clone, serde::Serialize)]
pub struct Stats { pub methods: u64, pub classes: u64, pub vectors: u64, pub last_build: Option<String> }

#[derive(Debug, Clone, serde::Serialize)]
pub struct DirStat { pub dir: String, pub methods: u64 }

#[derive(Debug, Clone, serde::Serialize)]
pub struct LangStat { pub ext: String, pub pct: u8 }

#[derive(Debug, Clone, serde::Serialize)]
pub struct KeyEntity { pub name: String, pub kind: String, pub source: String, pub in_degree: u64 }

#[derive(Debug, Clone, serde::Serialize)]
pub struct Candidate { pub path: String, pub suggested_name: String, pub build_cmd: String }

#[derive(Debug, Clone, serde::Serialize)]
pub struct SenseReport {
    pub status: SenseStatus,
    pub project: Option<ProjectRef>,
    pub stats: Option<Stats>,
    pub dirs: Vec<DirStat>,
    pub languages: Vec<LangStat>,
    pub key_entities: Vec<KeyEntity>,
    pub candidates: Vec<Candidate>,
    pub degraded: Vec<String>,
}

pub struct SenseService {
    pub graph: Option<std::sync::Arc<dyn crate::domain::traits::GraphRepository>>,
    pub vector: Option<std::sync::Arc<dyn crate::domain::traits::VectorRepository>>,
    pub snapshot: Option<std::sync::Arc<dyn crate::domain::traits::SnapshotRepository>>,
}

impl SenseService {
    /// 永不失败：后端缺失走 degraded。input 已存在性由调用方（CLI）校验。
    pub async fn sense(
        &self,
        input: &std::path::Path,
        projects: &[(String, std::path::PathBuf)],
    ) -> SenseReport;
}
```

- [x] **Step 1: 写失败测试（stubs + 三态）**

`src/application/sense/mod.rs` 末尾追加：

```rust
#[cfg(test)]
pub(crate) mod stubs {
    use super::*;
    use crate::domain::error::DtError;
    use crate::domain::traits::{GraphRepository, HealthStatus, SnapshotRepository, VectorRepository};
    use async_trait::async_trait;
    use std::collections::HashMap;

    pub struct StubGraph {
        /// read_query 响应脚本：按 query 含的关键词匹配返回固定 JSON。
        pub method_count: u64,
        pub class_count: u64,
        pub calls_rows: Vec<serde_json::Value>,
        pub heuristic_rows: Vec<serde_json::Value>,
    }

    #[async_trait]
    impl GraphRepository for StubGraph {
        async fn read_query(&self, query: &str, _params: HashMap<String, serde_json::Value>) -> Result<serde_json::Value, DtError> {
            if query.contains("CALLS") {
                Ok(serde_json::Value::Array(self.calls_rows.clone()))
            } else if query.contains("ENDS WITH") {
                Ok(serde_json::Value::Array(self.heuristic_rows.clone()))
            } else if query.contains("Method") {
                Ok(serde_json::json!([{ "c": self.method_count }]))
            } else if query.contains("Class") {
                Ok(serde_json::json!([{ "c": self.class_count }]))
            } else {
                Ok(serde_json::json!([]))
            }
        }
        async fn write_query(&self, _q: &str, _p: HashMap<String, serde_json::Value>) -> Result<serde_json::Value, DtError> { Ok(serde_json::Value::Null) }
        async fn health_check(&self) -> Result<HealthStatus, DtError> { Ok(HealthStatus::Healthy) }
    }

    pub struct StubVector {
        pub payloads: Vec<serde_json::Value>,
    }

    #[async_trait]
    impl VectorRepository for StubVector {
        async fn ensure_collection(&self, _c: &str, _d: u32) -> Result<(), DtError> { Ok(()) }
        async fn search(&self, _c: &str, _v: Vec<f32>, _l: u64) -> Result<Vec<serde_json::Value>, DtError> { Ok(vec![]) }
        async fn upsert(&self, _c: &str, _p: Vec<serde_json::Value>) -> Result<(), DtError> { Ok(()) }
        async fn delete_by_filter(&self, _c: &str, _f: serde_json::Value) -> Result<u64, DtError> { Ok(0) }
        async fn list_collections(&self) -> Result<Vec<String>, DtError> { Ok(vec![]) }
        async fn collection_info(&self, _c: &str) -> Result<serde_json::Value, DtError> { Ok(serde_json::Value::Null) }
        async fn delete_collection(&self, _c: &str) -> Result<(), DtError> { Ok(()) }
        async fn health_check(&self) -> Result<HealthStatus, DtError> { Ok(HealthStatus::Healthy) }
        async fn scroll_payloads(&self, _c: &str, _f: Option<serde_json::Value>, _max: usize) -> Result<Vec<serde_json::Value>, DtError> {
            Ok(self.payloads.clone())
        }
    }

    pub struct StubSnapshot {
        pub updated_at: Option<String>,
    }

    #[async_trait]
    impl SnapshotRepository for StubSnapshot {
        async fn get_snapshot(&self, _p: &str, _path: &str) -> Result<Option<crate::domain::types::FileSnapshot>, DtError> { Ok(None) }
        async fn save_snapshots(&self, _p: &str, _s: &[crate::domain::types::FileSnapshot]) -> Result<(), DtError> { Ok(()) }
        async fn delete_project(&self, _p: &str) -> Result<u64, DtError> { Ok(0) }
        async fn list_snapshots(&self, project: &str) -> Result<Vec<crate::domain::types::FileSnapshot>, DtError> {
            Ok(match &self.updated_at {
                Some(ts) => vec![crate::domain::types::FileSnapshot {
                    file_path: "a.java".into(), project: project.into(), file_sha1: "h".into(),
                    file_mtime: 0.0, method_count: 1, updated_at: ts.clone(),
                }],
                None => vec![],
            })
        }
        async fn mark_llm_analyzed(&self, _p: &str, _f: &str, _h: &str) -> Result<(), DtError> { Ok(()) }
        async fn is_llm_analyzed(&self, _p: &str, _f: &str, _h: &str) -> Result<bool, DtError> { Ok(false) }
        async fn clear_llm_progress(&self, _p: &str) -> Result<(), DtError> { Ok(()) }
    }
}
```

注意：`HealthStatus` 的真实路径以 `src/domain/traits.rs` 中定义为准（`rg -n "enum HealthStatus" src/domain/traits.rs` 确认 import 路径）；若 trait 还有其他必需方法（以编译器报错为准）按 "返回空/Ok(())" 补齐。`VectorRepository`/`SnapshotRepository` 必需方法清单以 `src/domain/traits.rs` :34-70 与 :146-247 实际声明为准补齐 stub。

测试本体（`mod.rs` 继续追加）：

```rust
#[cfg(test)]
mod status_tests {
    use super::*;
    use stubs::*;
    use std::path::{Path, PathBuf};
    use std::sync::Arc;

    fn projects() -> Vec<(String, PathBuf)> {
        vec![("dt".into(), PathBuf::from("/data/myProject/digital-twin-v2"))]
    }

    fn svc(vectors: usize, methods: u64) -> SenseService {
        let payloads = (0..vectors)
            .map(|i| serde_json::json!({"file_path": format!("/data/myProject/digital-twin-v2/src/m{}.rs", i), "project": "dt"}))
            .collect();
        SenseService {
            graph: Some(Arc::new(StubGraph { method_count: methods, class_count: 3, calls_rows: vec![], heuristic_rows: vec![] })),
            vector: Some(Arc::new(StubVector { payloads })),
            snapshot: Some(Arc::new(StubSnapshot { updated_at: Some("2026-08-01T10:00:00".into()) })),
        }
    }

    #[tokio::test]
    async fn indexed_when_vectors_exist() {
        let r = svc(10, 8).sense(Path::new("/data/myProject/digital-twin-v2"), &projects()).await;
        assert_eq!(r.status, SenseStatus::Indexed);
        let s = r.stats.unwrap();
        assert_eq!(s.vectors, 10);
        assert_eq!(s.methods, 8);
        assert_eq!(s.classes, 3);
        assert_eq!(s.last_build.as_deref(), Some("2026-08-01T10:00:00"));
        assert!(r.degraded.is_empty());
    }

    #[tokio::test]
    async fn registered_not_indexed_when_empty() {
        let r = svc(0, 0).sense(Path::new("/data/myProject/digital-twin-v2"), &projects()).await;
        assert_eq!(r.status, SenseStatus::RegisteredNotIndexed);
        assert_eq!(r.project.as_ref().unwrap().name, "dt");
    }

    #[tokio::test]
    async fn unregistered_when_no_match() {
        let r = svc(0, 0).sense(Path::new("/home/luis/x"), &projects()).await;
        assert_eq!(r.status, SenseStatus::Unregistered);
        assert!(r.project.is_none());
    }

    #[tokio::test]
    async fn memgraph_fallback_when_vector_down() {
        let mut s = svc(0, 5);
        s.vector = None; // Qdrant 不可用
        let r = s.sense(Path::new("/data/myProject/digital-twin-v2"), &projects()).await;
        assert_eq!(r.status, SenseStatus::Indexed); // Memgraph 方法数 >0 兜底
        assert!(r.degraded.contains(&"qdrant".to_string()));
    }

    #[tokio::test]
    async fn all_backends_down_still_registered_not_indexed() {
        let s = SenseService { graph: None, vector: None, snapshot: None };
        let r = s.sense(Path::new("/data/myProject/digital-twin-v2"), &projects()).await;
        assert_eq!(r.status, SenseStatus::RegisteredNotIndexed);
        assert_eq!(r.degraded.len(), 3);
    }
}
```

- [x] **Step 2: 运行确认失败**

Run: `cargo test sense 2>&1 | tail -5`
Expected: FAIL（SenseService 未实现）

- [x] **Step 3: 实现 SenseService::sense（status + stats 部分；dirs/languages/key_entities/candidates 留空 Vec 待 T4-T6 填）**

`mod.rs` 追加（`pub mod locate;` 之后）：

```rust
use std::path::{Path, PathBuf};
use std::sync::Arc;
use crate::domain::traits::{GraphRepository, SnapshotRepository, VectorRepository};

// …类型定义（见 Produces）…

impl SenseService {
    pub async fn sense(&self, input: &Path, projects: &[(String, PathBuf)]) -> SenseReport {
        let mut degraded: Vec<String> = Vec::new();

        let Some((name, root)) = locate::match_project(input, projects) else {
            return SenseReport {
                status: SenseStatus::Unregistered,
                project: None,
                stats: None,
                dirs: vec![], languages: vec![], key_entities: vec![],
                candidates: vec![], // T6 填充
                degraded,
            };
        };

        // --- stats: Memgraph 方法/类计数 ---
        let mut methods = 0u64;
        let mut classes = 0u64;
        if let Some(g) = &self.graph {
            let mut params = std::collections::HashMap::new();
            params.insert("p".to_string(), serde_json::Value::String(name.to_string()));
            if let Ok(v) = g.read_query(
                "MATCH (m:Method {project: $p}) RETURN count(m) AS c", params.clone()).await {
                methods = v.as_array().and_then(|a| a.first())
                    .and_then(|r| r.get("c")).and_then(|c| c.as_u64()).unwrap_or(0);
            }
            if let Ok(v) = g.read_query(
                "MATCH (c:Class {project: $p}) RETURN count(c) AS c", params).await {
                classes = v.as_array().and_then(|a| a.first())
                    .and_then(|r| r.get("c")).and_then(|c| c.as_u64()).unwrap_or(0);
            }
        } else {
            degraded.push("memgraph".into());
        }

        // --- stats: Qdrant 向量（同时是 T4 目录画像原料） ---
        let mut payloads: Vec<serde_json::Value> = vec![];
        if let Some(v) = &self.vector {
            let filter = serde_json::json!({"must": [{"key": "project", "match": {"value": name}}]});
            match v.scroll_payloads("code_methods", Some(filter), 100_000).await {
                Ok(p) => payloads = p,
                Err(_) => degraded.push("qdrant".into()),
            }
        } else {
            degraded.push("qdrant".into());
        }
        let vectors = payloads.len() as u64;

        // --- stats: SQLite 最近构建 ---
        let mut last_build = None;
        if let Some(s) = &self.snapshot {
            if let Ok(snaps) = s.list_snapshots(name).await {
                last_build = snaps.into_iter().map(|f| f.updated_at).max();
            }
        } else {
            degraded.push("snapshot".into());
        }

        // --- 三态（S-D2 + spec §6 Qdrant 缺失时 Memgraph 兜底） ---
        let status = if vectors > 0 || methods > 0 {
            SenseStatus::Indexed
        } else {
            SenseStatus::RegisteredNotIndexed
        };

        SenseReport {
            status,
            project: Some(ProjectRef { name: name.to_string(), path: root.display().to_string(), registered: true }),
            stats: Some(Stats { methods, classes, vectors, last_build }),
            dirs: vec![], languages: vec![], key_entities: vec![], candidates: vec![],
            degraded,
        }
    }
}
```

- [x] **Step 4: 运行测试通过 + 回归**

Run: `cargo test sense 2>&1 | tail -3 && cargo test --lib 2>&1 | tail -3`
Expected: sense 5 passed；lib 基线不变

- [x] **Step 5: Commit**

```bash
git add src/application/sense/
git commit -m "feat(dt-sense): SenseReport 契约 + SenseService 三态判定 + stats 采集"
```

---

### Task 4: 目录画像 + 语言分布聚合（纯函数）

**Files:**
- Create: `src/application/sense/brief.rs`
- Modify: `src/application/sense/mod.rs`（`pub mod brief;` + sense() 内接线 2 行）

**Interfaces:**
- Consumes: T3 `payloads`（scroll 返回的裸 payload JSON，`file_path` 为绝对路径）
- Produces:
```rust
/// 由 payloads 聚合目录画像与语言分布。
/// dir 口径：file_path 去掉 project_root 前缀后的第一级目录；根级文件归 "."。
pub fn aggregate_dirs(
    payloads: &[serde_json::Value],
    project_root: &str,
) -> (Vec<crate::application::sense::DirStat>, Vec<crate::application::sense::LangStat>);
```

- [x] **Step 1: 写失败测试**

`src/application/sense/brief.rs`：

```rust
//! 简报聚合：目录画像、语言分布、关键实体（纯函数 + Cypher 查询）。

use crate::application::sense::{DirStat, LangStat};

pub fn aggregate_dirs(payloads: &[serde_json::Value], project_root: &str) -> (Vec<DirStat>, Vec<LangStat>) {
    use std::collections::HashMap;
    let root = project_root.trim_end_matches('/');
    let mut dir_counts: HashMap<String, u64> = HashMap::new();
    let mut ext_counts: HashMap<String, u64> = HashMap::new();
    let mut total = 0u64;

    for p in payloads {
        let Some(fp) = p.get("file_path").and_then(|v| v.as_str()) else { continue };
        total += 1;
        let rel = fp.strip_prefix(root).unwrap_or(fp).trim_start_matches('/');
        let first = rel.split('/').next().unwrap_or("");
        let is_root_file = !rel.contains('/');
        let dir = if is_root_file { ".".to_string() } else { first.to_string() };
        *dir_counts.entry(dir).or_default() += 1;
        if let Some(ext) = rel.rsplit('.').next().filter(|e| e.contains(char::is_alphanumeric)) {
            if rel.contains('.') {
                *ext_counts.entry(ext.to_string()).or_default() += 1;
            }
        }
    }

    let mut dirs: Vec<DirStat> = dir_counts
        .into_iter()
        .map(|(dir, methods)| DirStat { dir, methods })
        .collect();
    dirs.sort_by(|a, b| b.methods.cmp(&a.methods).then(a.dir.cmp(&b.dir)));
    dirs.truncate(10);

    let mut languages: Vec<LangStat> = ext_counts
        .into_iter()
        .map(|(ext, n)| LangStat { ext, pct: ((n * 100 + total / 2) / total.max(1)) as u8 })
        .collect();
    languages.sort_by(|a, b| b.pct.cmp(&a.pct).then(a.ext.cmp(&b.ext)));

    (dirs, languages)
}

#[cfg(test)]
mod tests {
    use super::*;

    fn pl(paths: &[&str]) -> Vec<serde_json::Value> {
        paths.iter().map(|p| serde_json::json!({"file_path": p})).collect()
    }

    #[test]
    fn aggregates_first_level_dirs() {
        let payloads = pl(&[
            "/data/p/src/a.java", "/data/p/src/b.java",
            "/data/p/web/c.ts",
            "/data/p/README.md",
        ]);
        let (dirs, _) = aggregate_dirs(&payloads, "/data/p");
        assert_eq!(dirs[0].dir, "src");
        assert_eq!(dirs[0].methods, 2);
        assert!(dirs.iter().any(|d| d.dir == "web" && d.methods == 1));
        assert!(dirs.iter().any(|d| d.dir == "." && d.methods == 1));
    }

    #[test]
    fn languages_pct_sums_roughly_100() {
        let payloads = pl(&["/p/a.java", "/p/b.java", "/p/c.java", "/p/d.ts"]);
        let (_, langs) = aggregate_dirs(&payloads, "/p");
        assert_eq!(langs[0].ext, "java");
        assert_eq!(langs[0].pct, 75);
        assert_eq!(langs[1].pct, 25);
    }

    #[test]
    fn empty_payloads_empty_output() {
        let (dirs, langs) = aggregate_dirs(&[], "/p");
        assert!(dirs.is_empty() && langs.is_empty());
    }
}
```

- [x] **Step 2: 运行确认失败**

Run: `cargo test brief 2>&1 | tail -3`
Expected: FAIL（模块不存在）

- [x] **Step 3: 接线进 sense()**

`mod.rs` 加 `pub mod brief;`；`sense()` 中 `dirs: vec![], languages: vec![]` 改为（仅 indexed 时聚合）：

```rust
let (dirs, languages) = if status == SenseStatus::Indexed {
    brief::aggregate_dirs(&payloads, &root.display().to_string())
} else {
    (vec![], vec![])
};
```

注意：`status` 计算依赖 `payloads`，需把 status 判定行移到 dirs/languages 赋值之前（当前顺序已满足）。

- [x] **Step 4: 运行测试通过**

Run: `cargo test sense 2>&1 | tail -3`
Expected: brief 3 + status 5 全过

- [x] **Step 5: Commit**

```bash
git add src/application/sense/
git commit -m "feat(dt-sense): 目录画像 + 语言分布聚合"
```

---

### Task 5: 关键实体（CALLS 入度 → 名称启发式降级）

**Files:**
- Modify: `src/application/sense/brief.rs`（追加 `key_entities`）
- Modify: `src/application/sense/mod.rs`（sense() 接线）

**Interfaces:**
- Consumes: `GraphRepository::read_query`；T3 stubs 已支持 `CALLS`/`ENDS WITH` 脚本分发
- Produces:
```rust
/// 关键实体：CALLS 入度 top10；0 行 → Class 名称启发式；仍空 → vec![]。
pub async fn key_entities(
    graph: &dyn crate::domain::traits::GraphRepository,
    project: &str,
) -> Vec<crate::application::sense::KeyEntity>;
```

- [x] **Step 1: 写失败测试**

`brief.rs` tests 追加：

```rust
#[cfg(test)]
mod key_entity_tests {
    use crate::application::sense::stubs::StubGraph;
    use crate::application::sense::brief::key_entities;
    use std::sync::Arc;

    #[tokio::test]
    async fn in_degree_top10() {
        let g = StubGraph {
            method_count: 0, class_count: 0,
            calls_rows: vec![serde_json::json!({"name": "handle_build", "d": 12})],
            heuristic_rows: vec![],
        };
        let out = key_entities(&g, "p").await;
        assert_eq!(out.len(), 1);
        assert_eq!(out[0].name, "handle_build");
        assert_eq!(out[0].in_degree, 12);
        assert_eq!(out[0].source, "in_degree");
        assert_eq!(out[0].kind, "method");
    }

    #[tokio::test]
    async fn heuristic_fallback() {
        let g = StubGraph {
            method_count: 0, class_count: 0,
            calls_rows: vec![],
            heuristic_rows: vec![serde_json::json!({"name": "PayController"})],
        };
        let out = key_entities(&g, "p").await;
        assert_eq!(out.len(), 1);
        assert_eq!(out[0].source, "heuristic");
        assert_eq!(out[0].kind, "class");
    }

    #[tokio::test]
    async fn both_empty_returns_empty() {
        let g = StubGraph { method_count: 0, class_count: 0, calls_rows: vec![], heuristic_rows: vec![] };
        assert!(key_entities(&g, "p").await.is_empty());
    }
}
```

`key_entities` 是 async 但 stub 直接实现了 trait——测试调用 `key_entities(&g, "p")`，`&StubGraph as &dyn GraphRepository` 自动转型。

- [x] **Step 2: 运行确认失败**

Run: `cargo test key_entities 2>&1 | tail -3`
Expected: FAIL（函数不存在）

- [x] **Step 3: 实现**

`brief.rs` 追加：

```rust
use crate::application::sense::KeyEntity;
use crate::domain::traits::GraphRepository;

pub async fn key_entities(graph: &dyn GraphRepository, project: &str) -> Vec<KeyEntity> {
    let mut params = std::collections::HashMap::new();
    params.insert("p".to_string(), serde_json::Value::String(project.to_string()));

    // 首选：CALLS 入度
    let in_degree = graph
        .read_query(
            "MATCH (m:Method {project: $p})<-[:CALLS]-()              RETURN m.name AS name, count(*) AS d ORDER BY d DESC LIMIT 10",
            params.clone(),
        )
        .await
        .ok()
        .and_then(|v| v.as_array().cloned())
        .unwrap_or_default();
    if !in_degree.is_empty() {
        return in_degree
            .iter()
            .map(|r| KeyEntity {
                name: r.get("name").and_then(|n| n.as_str()).unwrap_or("?").to_string(),
                kind: "method".into(),
                source: "in_degree".into(),
                in_degree: r.get("d").and_then(|d| d.as_u64()).unwrap_or(0),
            })
            .collect();
    }

    // 降级：Class 名称启发式
    let heuristic = graph
        .read_query(
            "MATCH (c:Class {project: $p})              WHERE c.name ENDS WITH 'Controller' OR c.name ENDS WITH 'Service'                 OR c.name ENDS WITH 'Mapper' OR c.name ENDS WITH 'Application'              RETURN c.name AS name LIMIT 10",
            params,
        )
        .await
        .ok()
        .and_then(|v| v.as_array().cloned())
        .unwrap_or_default();
    heuristic
        .iter()
        .map(|r| KeyEntity {
            name: r.get("name").and_then(|n| n.as_str()).unwrap_or("?").to_string(),
            kind: "class".into(),
            source: "heuristic".into(),
            in_degree: 0,
        })
        .collect()
}
```

- [x] **Step 4: 接线 + 回归**

`mod.rs` sense() 中 `key_entities: vec![]` 改为（graph 存在且 indexed 时）：

```rust
let key_entities = if status == SenseStatus::Indexed {
    match &self.graph {
        Some(g) => brief::key_entities(g.as_ref(), name).await,
        None => vec![],
    }
} else {
    vec![]
};
```

Run: `cargo test sense 2>&1 | tail -3 && cargo test --lib 2>&1 | tail -3 && cargo clippy 2>&1 | grep -E "^error" | head -3`
Expected: 全过；基线不变；clippy 0 error

- [x] **Step 5: Commit**

```bash
git add src/application/sense/
git commit -m "feat(dt-sense): 关键实体（CALLS 入度 + 名称启发式降级）"
```

---

### Task 6: discover 候选项目扫描

**Files:**
- Create: `src/application/sense/discover.rs`
- Modify: `src/application/sense/mod.rs`（`pub mod discover;` + sense() unregistered 分支接线）

**Interfaces:**
- Consumes: T3 `Candidate`
- Produces:
```rust
/// 扫描 dir 的一级子目录，返回含源码的候选项目（按 path 排序）。
/// 排除：ScanConfig::default().ignore_dirs 目录名 + ignored_file 中列出的绝对路径。
pub fn scan_candidates(
    dir: &std::path::Path,
    ignored_dirs_file: &std::path::Path,
) -> Vec<crate::application::sense::Candidate>;
```

- [x] **Step 1: 写失败测试**

`src/application/sense/discover.rs`：

```rust
//! 候选项目发现：未注册目录的一级子目录扫描（depth<=2 找源码文件）。

use crate::application::sense::Candidate;
use std::path::{Path, PathBuf};

const SOURCE_EXTS: &[&str] = &["java", "py", "ts", "tsx", "js", "jsx", "go", "rs", "php"];

pub fn scan_candidates(dir: &Path, ignored_dirs_file: &Path) -> Vec<Candidate> {
    let extra_ignored = read_ignored_file(ignored_dirs_file);
    let default_ignored: std::collections::HashSet<String> = crate::domain::types::ScanConfig::default().ignore_dirs;

    let mut out = Vec::new();
    let Ok(entries) = std::fs::read_dir(dir) else { return out };
    for entry in entries.flatten() {
        let path = entry.path();
        if !path.is_dir() { continue; }
        let name = entry.file_name().to_string_lossy().to_string();
        if default_ignored.contains(&name) || name.starts_with('.') { continue; }
        if extra_ignored.iter().any(|p| p == &path) { continue; }
        if has_source_within(&path, 2) {
            out.push(Candidate {
                path: path.display().to_string(),
                suggested_name: name.clone(),
                build_cmd: format!("dt build --path {} --name {}", path.display(), name),
            });
        }
    }
    out.sort_by(|a, b| a.path.cmp(&b.path));
    out
}

/// ignored_dirs.yaml：逐行绝对路径（# 开头为注释）。
fn read_ignored_file(path: &Path) -> Vec<PathBuf> {
    std::fs::read_to_string(path)
        .map(|c| {
            c.lines()
                .map(|l| l.trim())
                .filter(|l| !l.is_empty() && !l.starts_with('#'))
                .map(PathBuf::from)
                .collect()
        })
        .unwrap_or_default()
}

fn has_source_within(dir: &Path, depth: u8) -> bool {
    if depth == 0 { return false; }
    let default_ignored: std::collections::HashSet<String> = crate::domain::types::ScanConfig::default().ignore_dirs;
    let Ok(entries) = std::fs::read_dir(dir) else { return false; };
    for entry in entries.flatten() {
        let path = entry.path();
        let name = entry.file_name().to_string_lossy().to_string();
        if path.is_dir() {
            if !default_ignored.contains(&name) && !name.starts_with('.') && has_source_within(&path, depth - 1) {
                return true;
            }
        } else if let Some(ext) = path.extension().and_then(|e| e.to_str()) {
            if SOURCE_EXTS.contains(&ext) {
                return true;
            }
        }
    }
    false
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn finds_source_dirs_and_skips_noise() {
        let tmp = std::env::temp_dir().join(format!("dt-sense-test-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&tmp);
        std::fs::create_dir_all(tmp.join("proj-a/src")).unwrap();
        std::fs::write(tmp.join("proj-a/src/Main.java"), "class Main {}").unwrap();
        std::fs::create_dir_all(tmp.join("node_modules/dep")).unwrap();
        std::fs::write(tmp.join("node_modules/dep/x.js"), "x").unwrap();
        std::fs::create_dir_all(tmp.join("docs-only")).unwrap();
        std::fs::write(tmp.join("docs-only/README.md"), "doc").unwrap();
        std::fs::create_dir_all(tmp.join("proj-b")).unwrap(); // 无源码
        std::fs::write(tmp.join("proj-b/config.yaml"), "x: 1").unwrap();

        let ignored = tmp.join("ignored.yaml");
        std::fs::write(&ignored, format!("# comment\n{}\n", tmp.join("proj-c").display())).unwrap();
        std::fs::create_dir_all(tmp.join("proj-c")).unwrap();
        std::fs::write(tmp.join("proj-c/app.py"), "x=1").unwrap();

        let out = scan_candidates(&tmp, &ignored);
        let names: Vec<&str> = out.iter().map(|c| c.suggested_name.as_str()).collect();
        assert_eq!(names, vec!["proj-a"]);
        assert!(out[0].build_cmd.contains("dt build --path"));
        assert!(out[0].build_cmd.contains("--name proj-a"));
        let _ = std::fs::remove_dir_all(&tmp);
    }

    #[test]
    fn depth2_source_counts() {
        let tmp = std::env::temp_dir().join(format!("dt-sense-test2-{}", std::process::id()));
        let _ = std::fs::remove_dir_all(&tmp);
        std::fs::create_dir_all(tmp.join("deep/src/main")).unwrap();
        std::fs::write(tmp.join("deep/src/main/app.go"), "package main").unwrap();
        let out = scan_candidates(&tmp, &tmp.join("none.yaml"));
        assert_eq!(out.len(), 1);
        let _ = std::fs::remove_dir_all(&tmp);
    }
}
```

- [x] **Step 2: 运行确认失败**

Run: `cargo test discover 2>&1 | tail -3`
Expected: FAIL（模块不存在）

- [x] **Step 3: 接线进 sense()**

`mod.rs` 加 `pub mod discover;`；unregistered 分支 `candidates: vec![]` 改为：

```rust
candidates: discover::scan_candidates(
    input,
    &input.join(".config/digital-twin/ignored_dirs.yaml"),
),
```

ignored_dirs.yaml 真实路径是 `~/.config/digital-twin/ignored_dirs.yaml`——library 不读 home 会更干净：调用方（T7 CLI）解析 home 路径并传入。调整方案：`SenseService` 加字段 `pub ignored_dirs_file: Option<PathBuf>`，CLI 传 `dirs_like_home_config(".config/digital-twin/ignored_dirs.yaml")`；测试传 None。对应修改：
1. `mod.rs` SenseService 定义加字段（位置：`snapshot` 字段之后）
2. **T3 status_tests 的 `svc()` 帮助函数与 `all_backends_down_still_registered_not_indexed` 中的全部 SenseService 构造点**，统一加 `ignored_dirs_file: None`（共 2 处：`svc()` 内 1 处 + 直接构造 1 处）
3. unregistered 分支 candidates 改为 `discover::scan_candidates(input, self.ignored_dirs_file.as_deref().unwrap_or(Path::new("/nonexistent")))`
4. T5/T6 之后跑 `cargo test sense` 确认无漏改构造点（编译器会列出所有缺字段位置）

- [x] **Step 4: 运行测试通过 + 回归**

Run: `cargo test sense 2>&1 | tail -3 && cargo test --lib 2>&1 | tail -3`
Expected: 全过；基线不变

- [x] **Step 5: Commit**

```bash
git add src/application/sense/
git commit -m "feat(dt-sense): discover 候选项目扫描"
```

---

### Task 7: CLI 接线 + 文本渲染 + --json

**Files:**
- Create: `src/interfaces/cli/sense.rs`
- Modify: `src/interfaces/cli/mod.rs`（加 `pub mod sense;`）
- Modify: `src/main.rs`（Commands 加 Sense 变体 + dispatch；复用 :473 `load_config`、:497 `resolve_project_paths`、:556/:619/:739 connect_*）

**Interfaces:**
- Consumes: T3 `SenseService`/`SenseReport`；main.rs 的 `load_config()`/`resolve_project_paths()`/`connect_graph()`/`connect_vector()`/`connect_snapshot()`/`dirs_like_home_config()`
- Produces:
```rust
// main.rs Commands 变体：
/// Environment sensing — locate project, report index status and content brief.
Sense {
    /// Target directory (default: current working directory).
    path: Option<std::path::PathBuf>,
    /// Output pure JSON to stdout (for MCP / scripting).
    #[arg(long = "json")]
    json: bool,
},

// src/interfaces/cli/sense.rs
pub async fn handle_sense(
    path: Option<std::path::PathBuf>,
    json: bool,
    projects: Vec<(String, std::path::PathBuf)>,
    graph: Option<std::sync::Arc<dyn crate::domain::traits::GraphRepository>>,
    vector: Option<std::sync::Arc<dyn crate::domain::traits::VectorRepository>>,
    snapshot: Option<std::sync::Arc<dyn crate::domain::traits::SnapshotRepository>>,
    ignored_dirs_file: Option<std::path::PathBuf>,
) -> anyhow::Result<()>;
```

- [x] **Step 1: 实现 handle_sense + 渲染**

```rust
//! CLI handler for `dt sense` — environment sensing.

use crate::application::sense::{SenseService, SenseStatus};
use crate::domain::traits::{GraphRepository, SnapshotRepository, VectorRepository};
use std::path::PathBuf;
use std::sync::Arc;

pub async fn handle_sense(
    path: Option<PathBuf>,
    json: bool,
    projects: Vec<(String, PathBuf)>,
    graph: Option<Arc<dyn GraphRepository>>,
    vector: Option<Arc<dyn VectorRepository>>,
    snapshot: Option<Arc<dyn SnapshotRepository>>,
    ignored_dirs_file: Option<PathBuf>,
) -> anyhow::Result<()> {
    let input = match path {
        Some(p) => p,
        None => std::env::current_dir()?,
    };
    let input = input.canonicalize().unwrap_or(input);
    if !input.exists() {
        anyhow::bail!("path not found: {}", input.display());
    }

    let svc = SenseService { graph, vector, snapshot, ignored_dirs_file };
    let report = svc.sense(&input, &projects).await;

    if json {
        println!("{}", serde_json::to_string_pretty(&report)?);
        return Ok(());
    }

    // ---- human readable ----
    let status_str = match report.status {
        SenseStatus::Indexed => "indexed",
        SenseStatus::RegisteredNotIndexed => "registered (not built)",
        SenseStatus::Unregistered => "unregistered",
    };
    println!("📍 {}", input.display());
    println!("  Status: {status_str}");

    if let Some(p) = &report.project {
        println!("  Project: {} ({})", p.name, p.path);
    }
    if let Some(s) = &report.stats {
        println!(
            "  Stats:  methods={} classes={} vectors={} last_build={}",
            s.methods, s.classes, s.vectors,
            s.last_build.as_deref().unwrap_or("-"),
        );
    }
    if !report.languages.is_empty() {
        let langs: Vec<String> = report.languages.iter().take(5)
            .map(|l| format!(".{} {}%", l.ext, l.pct)).collect();
        println!("  Languages: {}", langs.join(" · "));
    }
    if !report.dirs.is_empty() {
        println!("  Top dirs:");
        for d in report.dirs.iter().take(8) {
            println!("    {:<40} {} methods", d.dir, d.methods);
        }
    }
    if !report.key_entities.is_empty() {
        println!("  Key entities:");
        for e in &report.key_entities {
            let suffix = if e.source == "in_degree" {
                format!("in-degree {}", e.in_degree)
            } else {
                e.kind.clone()
            };
            println!("    [{}] {} ({})", e.source, e.name, suffix);
        }
    }
    if report.status == SenseStatus::RegisteredNotIndexed {
        if let Some(p) = &report.project {
            println!("  💡 已注册未构建，建议: dt build --name {}", p.name);
        }
    }
    if !report.candidates.is_empty() {
        println!("  Candidates ({}):", report.candidates.len());
        for c in &report.candidates {
            println!("    {}  →  {}", c.path, c.build_cmd);
        }
    }
    if !report.degraded.is_empty() {
        println!("  ⚠ degraded: {}", report.degraded.join(", "));
    }
    Ok(())
}
```

- [x] **Step 2: main.rs 接线**

Commands 枚举（`Search` 变体之后）插入 Produces 中的 `Sense` 变体。dispatch（`dt search` arm 之后）：

```rust
// ---- CLI mode: dt sense ----
Some(Commands::Sense { path, json }) => {
    let cfg = load_config();
    let projects = cfg.as_ref().map(|c| resolve_project_paths(c)).unwrap_or_default();
    let graph = connect_graph().await;
    let vector = connect_vector().await;
    let snapshot = connect_snapshot().await;
    let ignored = dirs_like_home_config(".config/digital-twin/ignored_dirs.yaml");
    dt_daemon::interfaces::cli::sense::handle_sense(
        path, json, projects, graph, vector, snapshot, ignored,
    )
    .await?;
    return Ok(());
}
```

`src/interfaces/cli/mod.rs` 加 `pub mod sense;`。

注意 T6 Step 3 中 `SenseService` 已加 `ignored_dirs_file: Option<PathBuf>` 字段；本任务同步更新 sense/mod.rs 中 SenseService 定义与所有测试构造点（测试传 `ignored_dirs_file: None`）。

- [x] **Step 3: 编译 + 全量测试**

Run: `cargo build --release 2>&1 | tail -3 && cargo test --lib 2>&1 | tail -3 && cargo clippy 2>&1 | grep -E "^error" | head -3`
Expected: 编译过；675+ 基线不变；clippy 0 error

- [x] **Step 4a: 补 live 集成测试（`#[ignore]`）**

`src/interfaces/cli/sense.rs` 末尾追加：

```rust
#[cfg(test)]
mod live_tests {
    /// live：需 Memgraph :7688 + Qdrant :6334 在线；本仓库须已注册且已构建。
    /// Run: cargo test -- --ignored sense_live
    #[tokio::test]
    #[ignore]
    async fn sense_live_returns_valid_json_shape() {
        let out = std::process::Command::new(env!("CARGO_BIN_EXE_dt"))
            .args(["sense", "--json"])
            .output()
            .expect("run dt sense");
        assert!(out.status.success());
        let v: serde_json::Value = serde_json::from_slice(&out.stdout).expect("valid json");
        assert!(v.get("status").is_some());
        assert!(v.get("degraded").is_some());
        let status = v["status"].as_str().unwrap();
        assert!(["indexed", "registered_not_indexed", "unregistered"].contains(&status));
    }
}
```

（`env!("CARGO_BIN_EXE_dt")` 在 integration test 中可用；lib 内单测若取不到，改用 `std::env::var("CARGO_BIN_EXE_dt").unwrap_or_else(|_| "./target/release/dt".into())`，先 `cargo build` 再跑。）

- [x] **Step 4b: 活体冒烟（三个场景）**

```bash
./target/release/dt sense                                    # 本仓库：registered（索引状态视实际数据）
./target/release/dt sense --json | python3 -m json.tool | head -30
./target/release/dt sense /tmp                               # unregistered → candidates（或空）
./target/release/dt sense /nonexistent; echo "exit=$?"       # 唯一失败场景，exit != 0
```

Expected: 三态输出格式正确；`/nonexistent` 报错退出非 0；其余退出 0。

- [x] **Step 5: Commit**

```bash
git add src/interfaces/cli/sense.rs src/interfaces/cli/mod.rs src/main.rs src/application/sense/
git commit -m "feat(dt-sense): CLI dt sense 接线 + 文本/JSON 渲染"
```

---

### Task 8: MCP dt_sense + 五处文档更新

**Files:**
- Modify: `mcp/mcp-server.py`（工具表 + dispatch + 头部注释 24→25）
- Modify: `AGENTS.md`（执行顺序改写）
- Modify: `skill/SKILL.md`（核心流程表加行）
- Modify: `skill/guides/PROJECT-DISCOVERY.md`（降级说明）
- Modify: `README.md`（CLI 16→17 + 命令表 + AI 集成表 + 24→25）
- Modify: `CLAUDE.md`（16→17）

**Interfaces:**
- Consumes: T7 `dt sense [path] --json`
- Produces: MCP 第 25 工具 `dt_sense(path?: string)`

- [x] **Step 1: MCP 工具注册**

`mcp/mcp-server.py` 头部注释 `提供工具 (24个):` → `提供工具 (25个):`，并在 `搜索:` 行加 `dt_sense`：

```
  搜索: dt_search_kg, dt_search, dt_sense
```

工具表（`dt_search_kg` 定义之后）插入：

```python
        Tool(
            name="dt_sense",
            description="环境感知（会话开始时的第一个动作）：定位目录所属项目，返回项目简报（统计/目录画像/语言/关键实体）；未注册目录返回候选项目发现报告。",
            inputSchema={
                "type": "object",
                "properties": {
                    "path": {"type": "string", "description": "目标目录，缺省为当前工作目录"}
                }, "required": []
            }
        ),
```

dispatch（`elif name == "dt_search_kg":` 分支之后）：

```python
    elif name == "dt_sense":
        cmd = [DT_BIN, "sense"]
        if arguments.get("path"):
            cmd.append(arguments["path"])
        cmd += ["--json"]
        text = run_cmd(cmd, timeout=120)
```

- [x] **Step 2: AGENTS.md 执行顺序改写**

把现有「执行流程」4 步（最小环境感知→提取关键词→查 KG→深入探索）整段替换为：

```markdown
**执行流程：**
1. **环境感知（第一个动作）** — 调 MCP `dt_sense()`（CLI 降级 `dt sense --json`）：一条命令完成项目定位、注册匹配、索引状态检查；已索引返回项目简报（统计/目录画像/语言/关键实体），未注册返回候选项目发现报告。**不再手工读目录猜关键词。**
2. **按场景搜索** — 具体任务需求走 `dt_search` / `dt_search_kg`（见下方查询策略）
3. **深入探索** — 感知与搜索完成后，再深入代码/目录

> dt_sense 不可用时（MCP 掉线且 dt 不在 PATH）才回退旧流程：读根目录 → 提取关键词 → 查 KG。
```

下方「唯一不查的情况」段落保留不变。

- [x] **Step 3: SKILL.md 核心流程表加行**

场景表（`| 🎯 **Leader 工作流…` 之前）插入首行：

```markdown
| 🧭 **会话开始/进入新目录（环境感知）** | MCP: `dt_sense()` / CLI: `dt sense --json` |
```

- [x] **Step 4: PROJECT-DISCOVERY.md 降级说明**

文件头部 `> AI 每次进入新工作空间时执行此规则…` 引用块替换为：

```markdown
> **⚠️ 角色变更（dt sense P1 后）**：本文件的规则已固化进 `dt sense` 命令（未注册分支的候选发现逻辑）。
> AI 正常流程第一个动作是 `dt_sense`/`dt sense --json`；本文件仅作为 sense 未注册分支行为的参考文档，
> 以及 sense 不可用时的兜底手工流程。
```

- [x] **Step 5: README.md / CLAUDE.md 计数与命令表**

README.md：
- `## CLI 命令 (16 个)` → `(17 个)`
- 「搜索」表加行：`| `dt sense` | 环境感知：项目定位 + 索引状态 + 目录内容简报 |`
- `mcp/mcp-server.py，24 个工具` → `25 个工具`；`# DT MCP Server V2 (24 工具)` → `(25 工具)`
- AI 集成表加首行：`| 环境感知（会话开始） | `dt_sense` | `dt sense --json` |`

CLAUDE.md：`` `dt` with 16 commands`` → `17 commands`。

- [x] **Step 6: 验证 + Commit**

```bash
python3 -c "import ast; ast.parse(open('mcp/mcp-server.py').read()); print('syntax ok')"
rg -n "16 个|24 个|24 工具|16 commands" README.md CLAUDE.md mcp/mcp-server.py skill/ AGENTS.md
./target/release/dt --help | grep -c "^  [a-z]"   # 命令数核对（16 业务 + help = 17 行）
```

Expected: syntax ok；计数无残留；help 输出 17 行。

```bash
git add mcp/mcp-server.py AGENTS.md skill/SKILL.md skill/guides/PROJECT-DISCOVERY.md README.md CLAUDE.md
git commit -m "feat(dt-sense): MCP dt_sense + 文档流程更新（感知入口固化）"
```

---

## 实施记录

**执行**：2026-08-03，inline 执行（executing-plans），8/8 任务完成，每任务一提交。

| 任务 | 提交 | 结果 |
|------|------|------|
| T1 scroll_payloads | `d3aa1da` | 默认空实现 + Qdrant scroll（API 名实为 `qdrant.scroll(builder)`，非 `scroll_points`） |
| T2 locate | `ef50bb7` | 4 单测过（修正 E0716 临时值借用） |
| T3 契约+三态+stats | `51de485` | 5 单测过；stub 按真实 trait 签名修正（delete_by_filter 返回 ()、collection_info 返回 CollectionInfo、Snapshot 补 health_check）；`ignored_dirs_file` 字段提前到本任务加入（免二次改构造点） |
| T4 目录画像 | `83d2763` | 3 单测过 + status 测试补断言 |
| T5 关键实体 | `c79e679` | 3 单测过（in_degree/heuristic/empty） |
| T6 discover | `f186560` | 2 单测过；修正 depth 语义 bug（depth 限目录下钻层数，不限本层文件检查） |
| T7 CLI | `759a9dd` | 三场景冒烟：本仓库 registered_not_indexed（建议 dt build）、/tmp unregistered、/nonexistent exit=1；/data/aflmProjects/aflm 发现 4 候选；live 测试 sense_live 通过 |
| T8 MCP+文档 | `5b0ae71` | MCP 25 工具；AGENTS 执行流程改写为 dt_sense 第一动作；CLAUDE.md 两处计数修正 |

**终态**：lib 测试 693 passed / 2 failed（均为预存 ts_java + backup_sqlite，与本次无关）/ 1 ignored（live）；clippy 0 error。
**偏差**：无计划外变更；T1 API 名、T3 stub 签名、T6 depth 语义为计划内提示的实现期修正。
