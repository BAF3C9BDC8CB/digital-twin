# Hermes 项目上下文（digital-twin-v2）

> ⚠️ **本文件优先级最高**（Hermes 加载顺序：`.hermes.md` > `AGENTS.md` > `CLAUDE.md` > `.cursorrules`，只加载一种）。
> Hermes 会话加载本文件后**不再读取 AGENTS.md**，避免 KG 行为准则重复注入。

## Hermes 下的 KG 行为准则来源

**KG 行为准则由 dt-memory 插件 `system_prompt_block` 编程注入**（cwd 无关、每会话必生效），是 Hermes 环境下的权威源，**本文不重复写入**。需要时直接看注入即可。

Hermes 会话中若要查 KG / 读码遵循（检索统一用 `dt_search`，已融合原 dt_search_kg 的 KG 优先语义）：
- 代码定位：先 `dt_search(world=code, project=digital-twin-v2)` 定位，再读码验证。
- 记忆/配置/部署/历史：`dt_search(world=memory)` 一次查完（记忆统一全局）。
- 环境感知：`dt_sense()`。
- 用户说"记忆/记一下"：立即 `dt_memorize`。

## 项目特有事实（Hermes 下需要）

- **项目名**：`digital-twin-v2`；**路径**：`/data/myProject/digital-twin-v2`。本项目是 KG 工具链自身。
- **世界**：代码实体在 `world=code`、文档在 `world=doc`、记忆在 `world=memory`。
- **事件未落地**（2026-08-31 实证）：Memgraph 只有代码索引 + `:Knowledge` 记忆，无事件标签节点；无需手动 `dt event`。
- **LLM 并发**：`providers.siliconflow.max_concurrent: 48`（单 provider 统一走 SiliconFlow；glmcoding/openai_compatible 已移除，2026-09-05）。
- **API Key**：用环境变量，禁止写入 Git；配置空值占位。
- **单文件更新**：`scripts/opencode-after-edit.sh`（`dt build --file`），非全量替代品。

> 非 Hermes 环境（OpenCode / 子代理任务书 / 直接读目录）请读取 `/data/myProject/digital-twin-v2/AGENTS.md` —— 那里是完整的全局行为准则汇总。
